<%= form_for(@blag) do |f| %>
  <% if @blag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blag.errors.count, "error") %> prohibited this blag from being saved:</h2>

      <ul>
      <% @blag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
